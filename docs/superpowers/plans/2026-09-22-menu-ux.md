# Menu UX repair

Current scope: QR-at-table ordering, as implemented by the September 19 rebuild.

1. Public menu and detail: explain browsing mode and expose the existing QR scanner.
2. Table menu: open the real item picker in place, preserving table context.
3. Simplify card borders, improve names and unavailable labels, remove disabled add affordances from browsing mode.
4. Collapse customer header utilities into an accessible settings dialog.
5. Run lint/typecheck/tests/build, push, then inspect deployed desktop/mobile UI. Do not claim live order settlement without exercising it.

Catalog data (test item and incorrect product image) requires separate authenticated admin access; never hide records by hardcoded names in rendering code.
